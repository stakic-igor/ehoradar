import Event from './Event';

const UpcomingEvents = () => {
    return (
        <section className="events">
            Upcoming Events
            <Event />
            <Event />
            <Event />
        </section>
    );
};

export default UpcomingEvents;
