import Nav from './Nav';

const Header = () => {
    return (
        <header className='header'>
            <div className='logo'>EhoRadar</div>
            <Nav />
        </header>
    );
};

export default Header;
